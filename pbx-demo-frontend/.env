# Keep empty in local development to use Vite proxy (/api, /hubs).
# Set an absolute URL only when frontend and backend are hosted on different origins.
# VITE_API_BASE=http://172.16.104.130:5175/
VITE_API_BASE=https://localhost:7078
